Build an HTML/CSS/JS implementation from `examples/fixtures/modal-interaction.json`.

Requirements:
- Preserve the button and modal bounds.
- The modal starts hidden.
- Clicking the primary button shows the modal.
- Use `interaction_summary` and element visibility to decide behavior.
