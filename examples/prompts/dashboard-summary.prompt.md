Build a React dashboard from `examples/fixtures/dashboard-summary.json`.

Requirements:
- Keep it dense and operational, not marketing-like.
- Preserve sidebar, KPI cards and table bounds.
- Use semantic roles to choose nav/card/table components.
- Return React and CSS that reproduce the exported layout.
