Build a React implementation from `examples/fixtures/landing-hero.json`.

Requirements:
- Treat the JSON as the visual contract.
- Preserve coordinates, sizes, layer order and semantic roles.
- Use `llm_brief` before reading individual elements.
- Do not redesign the landing page.
- Return a single React component and CSS.
